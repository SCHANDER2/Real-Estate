import Image from "next/image";
import Link from "next/link";
import { ArrowUpRight, BedDouble, MapPin, Ruler } from "lucide-react";
import { Property, formatPrice } from "@/data/properties";

export function PropertyCard({ property }: { property: Property }) { return <article className="group overflow-hidden rounded-2xl border border-slate-100 bg-white shadow-luxe transition hover:-translate-y-1"><div className="relative h-56 overflow-hidden"><Image src={property.image} alt={property.title} fill className="object-cover transition duration-500 group-hover:scale-105" /><span className="absolute left-4 top-4 rounded-full bg-white px-3 py-1 text-xs font-bold text-navy">{property.type}</span></div><div className="p-5"><p className="text-xl font-bold text-navy">{formatPrice(property.price)}</p><h3 className="mt-1 font-display text-xl text-ink">{property.title}</h3><p className="mt-2 flex items-center gap-1 text-sm text-slate-500"><MapPin size={15} className="text-gold"/>{property.location}</p><div className="mt-5 flex gap-4 border-y border-slate-100 py-3 text-xs font-bold text-slate-500"><span className="flex items-center gap-1"><Ruler size={15} className="text-navy"/>{property.area.toLocaleString()} sq ft</span>{property.bedrooms && <span className="flex items-center gap-1"><BedDouble size={15} className="text-navy"/>{property.bedrooms} beds</span>}</div><Link className="mt-5 flex items-center justify-between text-sm font-bold text-navy hover:text-gold" href={`/properties/${property.id}`}>View property <ArrowUpRight size={17}/></Link></div></article>; }
